<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Ansible Demo</title>
</head>
<body>
    <h1>Hello from {{ inventory_hostname }}</h1>
    <p>Hostname: {{ ansible_hostname | default(inventory_hostname) }}</p>
    <p>OS Family: {{ ansible_os_family | default('unknown') }}</p>
</body>
</html>
