[defaults]
inventory = inventorise/production/hosts
remote_user = root
host_key_checking = False
interpreter_python = /usr/bin/python3
roles_path = /root/rhel9/roles

#[privilege_escalation]
#become = False #是否全局默认开启提权
#become_method = sudo
#become_user = root #提权到的目标用户
#become_ask_pass = False #提权时是否需要输入密码

