# 张文暄 | 简历
**求职意向**：云计算运维工程师 / SRE / 云原生运维 / 模型交付运维
**教育背景**：太原理工大学
**核心方向**：Kubernetes 集群运维、模型私有化交付、Linux 自动化运维、云原生安全、异构算力平台搭建

---

# 项目经验（结合你全部笔记深度丰富版）

## 项目一：企业大脑 RAG 系统私有化部署与模型交付运维
**项目描述**
基于国产化信创环境，完成企业大脑大模型 RAG 架构私有化离线交付，负责 Embedding、Reranker 模型服务的部署、优化、对接与稳定性保障，实现文本向量化、语义检索、精排全流程可用。

**技术栈**
Docker Compose、Containerd、Xinference、CPU 多核推理、离线镜像、模型挂载、Nginx 反向代理

**核心工作**
1. 完成 **Xinference CPU 版服务部署**，配置宿主机目录挂载、端口映射、持久化存储，解决离线环境模型加载问题。
2. 实现 **Embedding / Reranker 双模型离线注册与启动**，解决模型重名冲突、路径不匹配、权限不足等问题。
3. 优化 CPU 多核推理性能，配置线程数、内存限制、日志轮转，防止 OOM 与磁盘爆满。
4. 完成模型服务 API 调试，对接企业大脑主服务，实现向量生成、语义重排序、知识库检索能力。
5. 构建完整交付验证体系：服务健康检查、接口自动化测试、日志监控、异常快速定位机制。

**项目成果**
- 独立完成企业大脑私有化交付全流程，服务稳定运行，支撑大模型 RAG 检索业务。
- 解决离线部署、模型加载、权限、性能等 10+ 生产问题，交付通过率 100%。

---

## 项目二：Kubernetes 单节点集群运维与高可用保障
**项目描述**
负责 Kubernetes 单 Master 集群运维管理，涵盖集群故障排查、网络修复、运行时切换、弹性伸缩、安全沙箱、资源管控，保障集群稳定可用。

**技术栈**
K8s、Docker、Containerd、Flannel、Metrics Server、HPA、gVisor、RuntimeClass、ResourceQuota、LimitRange

**核心工作**
1. 定位并修复 **Flannel 网络插件镜像缺失导致集群雪崩故障**，恢复节点 NetworkUnavailable 问题，避免业务 Pod 大量驱逐。
2. 完成 **容器运行时从 Docker 迁移到 Containerd**，解决 CRI 兼容、cgroup 对齐、镜像仓库问题。
3. 部署 **Metrics Server** 监控集群指标，配置 HPA 自动扩缩容，实现负载波动时自动弹性伸缩。
4. 搭建 **gVisor 沙箱运行时**，配置 RuntimeClass，实现不可信业务隔离，提升安全性。
5. 配置 ResourceQuota & LimitRange，实现 Namespace 资源强管控，防止资源耗尽。
6. 完善集群排障体系：Pod 排错、事件查看、日志分析、端口监控、服务自愈。

**项目成果**
- 成功恢复生产级 K8s 集群雪崩故障，保障业务快速恢复。
- 实现集群自动弹性伸缩、安全沙箱隔离、资源精细化管控。
- 具备完整 K8s 集群运维、故障排查、架构优化能力。

---

## 项目三：昇腾 NPU 异构算力平台部署与运维
**项目描述**
完成昇腾 Atlas 系列服务器 NPU 物理机部署，实现驱动、固件、CANN 工具链安装与验证，为 AI 训练/推理提供异构算力支撑。

**技术栈**
Ubuntu、Ascend Driver、Firmware、CANN、npu-smi、DKMS、内核模块、环境变量配置

**核心工作**
1. 安装服务器依赖：gcc、make、linux-headers、dkms 等编译环境。
2. 部署 **Ascend 固件与驱动**，严格保证版本匹配，解决内核编译失败问题。
3. 加载 ascend 内核模块，配置环境变量，使 npu-smi 工具全局生效。
4. 使用 npu-smi info/topology/health 完成 NPU 状态、拓扑、健康检查。
5. 解决设备无法识别、驱动加载失败、固件版本不匹配、权限异常等问题。

**项目成果**
- 完成昇腾 NPU 从零到一全流程部署，可提供 AI 异构算力支持。
- 掌握国产化算力平台部署、排错、验证标准流程。

---

## 项目四：Web 化运维工具开发（替代 Bash 交互式输入）
**项目描述**
将企业部署流程中**命令行交互式输入 IP** 的传统方式，改造为 **Web 页面批量输入提交**，实现标准化、自动化、可视化运维操作。

**技术栈**
Nginx、FastAPI、Bash、Python、Systemd、前后端分离、多站点隔离

**核心工作**
1. 搭建 **Nginx + FastAPI + Bash** 标准化架构，实现网页批量输入 IP → 自动调用脚本。
2. 实现双项目**端口、目录、配置三重隔离**（80/8000 旧项目，81/8001 新项目）互不干扰。
3. 开发前端表单页面，支持多行 IP 输入、UTF-8 中文适配、一键提交执行。
4. 后端解析表单，分级调度 Ping 策略，调用系统命令执行并返回结果。
5. 解决大量生产问题：端口冲突、依赖缺失、DNS 异常、502 网关、404 页面、服务崩溃、中文乱码等。
6. 配置 Systemd 托管服务，实现开机自启、异常自动重启、常驻后台运行。

**项目成果**
- 完成运维操作从命令行交互 → Web 化自动化的升级改造。
- 可直接迁移到企业大脑部署流程，提升部署标准化与效率。
- 具备全栈运维能力：Linux、Nginx、Python、脚本、服务托管、故障排查。

---

需要我再帮你做：
1. **一页纸精简版简历（秋招大厂专用）**
2. **专业技能关键词高亮版**
3. **简历封面+自我介绍**

你要哪个我直接给你生成。